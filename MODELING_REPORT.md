# 2026 高教社杯 B 题：无线电干扰源自动定位与清除

## 0. 目标、信息结构与“最短”的定义

一次动作的虚拟耗时为

\[
T=\sum_k\frac{\|p_k-p_{k-1}\|}{5}
 +5N_m+N_s+3N_f+5N_c,
\]

其中，`N_m` 为检测次数，`N_s` 为相邻检测指令的换频次数，`N_f` 为失败清除次数，`N_c` 为成功清除次数。`/clear` 不改变当前检测频道。

题目没有给出干扰源位置、频道、接收半径、定向方向的概率分布，因此“期望时间最短”没有唯一数学定义。本文采用字典序极小极大准则：

1. 首先保证任意符合题设的案例中所有干扰源都被清除；
2. 在保证清除的策略中，依次减少最坏检测次数、移动路程和清除尝试次数；
3. 对无法解析求得的连续选址与访问次序，采用鲁棒信息量准则和开路径 2-opt 近似。

完整策略不是预先固定的 HTTP 列表。因为第 `k+1` 条指令取决于第 `k` 条反馈，所以“指令序列文件”应当是可执行策略程序；程序运行后保存的 JSONL 才是该案例的完整实际指令序列。

## 1. 问题一：定位多边形、直径与覆盖圆

记第 `i` 个检测点为

\[
S_i=(x_i,y_i),\qquad \hat\theta_i\in[0,2\pi),\qquad \varepsilon=\pi/180.
\]

令

\[
u_i^-=\bigl(\cos(\hat\theta_i-\varepsilon),
\sin(\hat\theta_i-\varepsilon)\bigr),\quad
u_i^+=\bigl(\cos(\hat\theta_i+\varepsilon),
\sin(\hat\theta_i+\varepsilon)\bigr).
\]

真实干扰源 `G` 必须落在向前的误差角域中：

\[
u_i^-\times(G-S_i)\ge0,\qquad
u_i^+\times(G-S_i)\le0.
\]

每次检测给出两个半平面。将所有半平面逐次用 Sutherland--Hodgman 算法裁剪，得到凸定位多边形

\[
P=\bigcap_{i=1}^n W(S_i,\hat\theta_i,\varepsilon).
\]

若只使用示向度且交集无界，则直径为无穷；本题中还可与已知目标圆 `B(0,1800)` 以及每个有效读数对应的 `B(S_i,1500)` 求交。程序使用圆的外切正多边形表示这些圆约束，只会略微扩大可行域，不会错误排除真源。

凸多边形的直径一定由两个顶点取得：

\[
D(P)=\max_{v_i,v_j\in V(P)}\|v_i-v_j\|.
\]

顶点较少时可用 `O(m^2)` 枚举；按逆时针排序后可用旋转卡壳降至 `O(m)`。

“以定位区域直径为直径的圆”不一定覆盖定位区域。等边三角形的直径为边长 `D`，但最小覆盖圆半径为 `D/sqrt(3)>D/2`，所以任何直径为 `D` 的圆都无法覆盖它。正确判据是计算最小包围圆

\[
C^*(P)=\min_{c}\max_{x\in P}\|x-c\|.
\]

其支撑集只有两点或三点：分别对应直径圆或三点外接圆。若 `R(C^*)<=20 m`，在圆心调用一次 `/clear` 才具有严格成功保证。

## 2. 问题二：第二检测点的鲁棒选址

### 2.1 第一次检测真正提供的信息

记全向源未知有效接收半径为 `rho`，题设给出 `1000<=rho<=1500`。在 `S_1` 得到示向度而不是 `near`，还说明

\[
5<r_1=\|G-S_1\|\le\rho.
\]

因此第一次读数后的严格可行集为

\[
P_1=B(0,1800)\cap
\{g:5<\|g-S_1\|\le1500\}\cap
W(S_1,\hat\theta_1,1^\circ).
\]

计算时可暂时加入 `r_1<=5` 的闭包以保持凸性。这只会使候选区域略微缩小，不会破坏保证。

### 2.2 保证再次接收的完整候选区域

对任一假设位置 `g in P_1`，与第一次成功接收相容的最小有效半径不是恒定的 1000 m，而是

\[
\rho_{min}(g)=\max\{1000,\|g-S_1\|\}.
\]

所以第二点保证收到信号的充要条件为

\[
\boxed{
\mathcal C_2=
\bigcap_{g\in P_1}
B\!\left(g,\max\{1000,\|g-S_1\|\}\right).
}
\]

旧模型 `intersection B(g,1000)` 是 `C_2` 的保守子集，而不是完整候选区域；它丢掉了“首次已在距离 `r_1` 处收到信号，故 `rho>=r_1`”这一信息。

若暂不利用目标圆对扇区的裁剪，并把 `r_1>5` 放宽为 `r_1>=0`，还可得到便于作图和初始化的闭式保证区域。令

\[
u^\pm=(\cos(\hat\theta_1\pm1^\circ),
       \sin(\hat\theta_1\pm1^\circ)),\qquad
A^\pm=S_1+1000u^\pm,
\]

则

\[
\boxed{
\mathcal C_2^0=B(S_1,1000)\cap B(A^-,1000)\cap B(A^+,1000).
}
\]

证明只需分别考察 `r<=1000` 与 `r>=1000`。前者到候选点的最坏距离在扇区圆弧端点或 `S_1` 取得；后者将不等式平方后得到

\[
\|q-S_1\|^2\le2r(q-S_1)\mathbin{\cdot}u,
\]

其最严格情形为 `r=1000` 和两个角度端点。目标圆或 `r_1>5` 只会删除可能源位置，因此严格 `C_2` 不小于 `C_2^0`。`S_1` 自身必在候选区域内，但同一地点重复检测的环境误差不变，不能提供新信息，故应排除零基线选点。

### 2.3 以最坏定位直径定义“较好”

题目只规定误差属于 `[-1^\circ,1^\circ]`，没有给出概率分布。因此 Fisher 信息只能作为局部代理，不能直接当作严格最优结论。对候选点 `q`、可能真源 `g` 和第二次误差 `e_2`，定义

\[
P_2(q,g,e_2)=P_1\cap B(q,1500)\cap
W\!\left(q,\arg(g-q)+e_2,1^\circ\right).
\]

若 `||q-g||<=5`，第二次检测返回 `near` 并可立即清除；否则用第一问的直径作为全局、非概率定位指标：

\[
J_D(q)=
\max_{\substack{g\in P_1,\ \|q-g\|>5\\
                  e_2\in[-1^\circ,1^\circ]}}
D\bigl(P_2(q,g,e_2)\bigr).
\]

最终采用字典序优化

\[
\boxed{
q^*=\arg\min_{q\in\mathcal C_2}
\left(J_D(q),\frac{\|q-p_{now}\|}{5}\right).
}
\]

即先使最坏定位直径最小，再在近似等价的点中减少移动时间。这与 Bayram 等提出的定位不确定度和单机器人行程双目标思想一致。

直接对连续半无限问题求解较慢。程序先用方位测量雅可比矩阵

\[
H(g,q)=\sum_i\frac{n_i n_i^T}{r_i^2},\qquad
n_i=(-\sin\theta_i,\cos\theta_i),
\]

最大化 `min_g lambda_min(H)` 来筛选候选点。对于有界小角误差，`lambda_min(H)` 控制线性化位置误差的最坏方向，同时兼顾交会角和站源距离；再对少量优选点显式计算 `J_D`。这比只最大化交会角或把无概率依据的 Fisher D 最优作为最终结论更符合题设。

收到第二个示向度后，以实际读数更新 `P_2`。若最小包围圆半径不超过 20 m，则在圆心清除；否则继续用相同集合更新与鲁棒选址方法增加检测点。

### 2.4 标准化算例

取 `S_1=(0,0)`、实测示向度为 $0^\circ$，且目标圆不裁剪 `0--1500 m` 初始扇区。对 `C_2^0` 内候选点先作信息矩阵筛选，再以角度步长 $0.5^\circ$ 对第二次误差和定位多边形直径复核。在保留 2 m 接收裕量的条件下得到一对关于示向轴对称的优选点

\[
q_\pm\approx(833.94,\ \pm548.22)
=998(\cos33.32^\circ,\ \pm\sin33.32^\circ).
\]

相应采样最坏定位直径约为 `112.01 m`；旧示例 `(750, +/-600)` 的相同指标约为 `118.72 m`。前者改善约 `5.65%`。该数值用于说明算法，不是对任意 `S_1` 和目标圆裁剪都固定不变的坐标；实际第二点始终由上面的 `C_2` 与 `J_D` 重新计算。

## 3. 问题三：全向干扰源

### 3.1 全局发现阶段

首先判断能否把发现点数从 7 降至 6。这等价于用 6 个半径 1000 m 的圆覆盖半径
1800 m 的目标圆。Bezdek 对“6 个单位圆能覆盖的最大同心圆”给出的全局最优半径为
约 `1.7988678`；将该尺度换算到本题，覆盖半径至少需要

\[
1800/1.7988678\approx1000.6294\text{ m}>1000\text{ m}.
\]

本文又以任意不对称六圆心为变量，用 Voronoi 顶点及目标圆边界上的两两等距点精确计算
给定布局的最大漏距，数值复现同一临界值。因此 6 点即使不从原 7 点中选取也无法保证发现
最小接收半径为 1000 m 的源；7 点是严格的最少点数。

任一全向源只要与某检测站距离不超过 1000 m 就必定被发现。采用原点和半径 1125 m 正六边形上的六个点。外圆边界上最不利点位于相邻两站中间，其距离为

\[
d_{max}=\sqrt{1800^2+1125^2-2\cdot1800\cdot1125\cos30^\circ}
=999.11\text{ m}<1000\text{ m}.
\]

内部由原点站覆盖，因此这 7 站覆盖整个目标圆。半径 1125 m 接近该对称构型的最短路线下限

\[
a_{min}=1800\cos30^\circ-
\sqrt{1000^2-1800^2\sin^230^\circ}=1122.96\text{ m}.
\]

访问顺序为原点、六边形连续六个顶点，开路径长度 `6750 m`，移动耗时 `1350 s`。

在每一站只扫描仍需确认的频道，并采用蛇形频道顺序，使到达下一站后的第一次检测尽量不换频：

- 已清除频道不再检测；
- 定位区域最小包围圆半径已经不超过 19 m 的频道不再检测；
- 已发现干扰源达到上限 16 个后，不再检测尚未出现的频道；
- 其余频道必须完成 7 站覆盖，才能判为不存在。

如果所有频道都一直未能提前排除，搜索阶段保守上界为

\[
T_{search,3}\le1350+7\times20\times5+(140-1)=2189\text{ s}.
\]

实际案例中已发现、已定位频道会被提前删除，因此通常明显小于该上界。

### 3.2 定位与清除阶段

对每个已发现频道维护

\[
P_j=B(0,1800)\cap\bigcap_{i=1}^j
[B(S_i,1500)\cap W(S_i,\hat\theta_i,1^\circ)].
\]

若 `R_MEC(P_j)<=19 m`，到最小包围圆圆心清除。否则在问题二的候选区域中选择下一站；因为该站距所有可能真源均小于 1000 m，全向信号一定可接收。最多进行若干次鲁棒交会。

实际日志显示，搜索阶段已有两条以上示向线时，很多频道的 `R_MEC` 仅略高于 20 m；旧策略仍为这些频道绕行增加一次测向，定位移动成为问题 3 的最大耗时项。修正后，当 `19<R_MEC<=40 m` 时先在 MEC 圆心调用一次 `/clear`。成功则省去一次长距离测向；失败只增加 3 s 光学探测时间，随后仍进入保证接收的测向与覆盖清除流程，因此不会降低最终清除保证。20 组、每组 10--16 个随机全向源的成对离线测试中，该规则 20 组均缩短时间，平均总虚拟时间由 `4394.62 s` 降至 `4169.80 s`。

后续保证接收测点进一步采用时间感知选择：候选点必须对完整可行域保持至少 2 m 的接收
裕量，并保留最优最坏信息量的至少 10%，再从中选取距机器狗当前位置最近者；无合格候选时
退回原最坏定位尺度选择。100 组相同场景的配对离线测试中，全清率均为 `100/100`，平均
虚拟时间从 `4400.2 s` 降至 `4096.6 s`，平均降幅 `6.67%`。

当已经发现题设上限 16 个不同频道后，未出现频道已可严格判定为不存在。程序立即跳过当前
扫描列表中这些频道，但仍保留对已知源的顺路测向；100 组固定 16 源测试中该规则无一组
变慢，平均再节省 `10.62 s`。直接停止整条路线虽均值更低，却在个别样例变慢约 `4.77%`，
故不采用该激进规则。

全部待处理频道的 MEC 圆心先用开路径 2-opt 排序；每处理完一个频道后，依据机器狗实际落点对剩余频道重新排序。现有 5 份问题 3 日志的成功清除点回放表明，仅改进访问顺序平均还可减少约 `97.5 s` 移动时间。

若继续测向的移动成本高于直接清除，可用清除半径 19 m 的三角格覆盖整个 `P_j`。格点最近邻距离为 `sqrt(3)*19`，覆盖半径为 19 m；按机器狗当前位置做最近邻排序后逐点 `/clear`，一旦成功立即停止。因为真源始终属于 `P_j`，此步骤提供最终的确定性保证。

## 4. 问题四：混合全向与定向干扰源

### 4.1 为什么问题三策略不能直接使用

定向源的 `no_signal` 可能表示检测站位于背向半平面，不能据此排除频道或空间区域。任何只在目标圆内部布站的方案也无法保证发现位于边界且朝外发射的干扰源。

### 4.2 定向半圆的有限覆盖

设定向源位置为 `G`，单位发射方向为 `d`。按最坏接收半径 1000 m，其可接收区域为半圆

\[
H(G,d)=\{q:\|q-G\|\le1000,(q-G)\cdot d\ge0\}.
\]

对站集 `Q`，记源点 `G` 周围 1000 m 内的站为

\[
Q_G=\{q\in Q:\|q-G\|\le1000\}.
\]

要使任意方向 `d` 的半圆 `H(G,d)` 都至少含一个站，等价于

\[
\boxed{G\in\operatorname{conv}(Q_G).}
\]

若该条件不成立，分离超平面给出一个使所有近邻站都落在背向半平面的方向；若条件成立，则对任意 `d` 至少有一个近邻站满足 `(q-G) dot d>=0`。利用这一比“覆盖 `B(0,2300)`”更直接的判据进行自由选址。

先从“原点加 25 个平移三角晶格点”的 26 站方案出发。逐一删除任一非中心站都会在离散网格上产生满足 `G` 不属于 `conv(Q_G)` 的显式反例；即使在距原点 2800 m 内的 31 个晶格候选点中重新选点并强制保留原点，位置--连续方向整数规划仍要求至少 25 个晶格点，即总数至少为 26。因此，25 站含中心布局必须跳出原晶格候选集。

在自由的双环参数族中联合搜索内外环点数、半径与错位角，得到

\[
\boxed{
Q=\{(0,0)\}
\cup\{980(\cos30^\circ k,\sin30^\circ k):k=0,\ldots,11\}
\cup\{1875(\cos(12.5^\circ+30^\circ k),\sin(12.5^\circ+30^\circ k)):k=0,\ldots,11\}.
}
\]

该布局由原点、12 个内环站和 12 个外环站组成，共 25 站；它相当于保留中心并重新选择其余 24 点，而不是在原晶格上机械删除一点。最远检测站半径为 1875 m，满足题面的机器狗坐标要求。

连续保证采用网格邻域证书而不是只检查采样点。对目标圆使用径向 5 m、角度 $0.25^\circ$ 的极坐标网格。取径向半步长 2.5 m、角向半步长 $0.125^\circ$，在最外层作保守放大可得

\[
h=\sqrt{1800^2+1802.5^2-2\cdot1800\cdot1802.5\cos0.125^\circ}+0.01
=4.6675\text{ m}.
\]

故任意连续点到最近网格点的距离不超过 `h`。在每个网格点 `z` 只保留距离不超过 `1000-h` 的站，其凸包边界到 `z` 的最小距离经 518401 个网格点验证仍不小于 `11.1109 m>h`。因此 `B(z,h)` 位于这些站的凸包内，并且邻域中任一点到这些站仍不超过 1000 m，由此覆盖整个连续目标圆。

访问次序采用开放端点 2-opt：先从原点开始，依次绕行内环和外环，完整路线长为 `18178.83 m`，比原 25 站平移晶格路线减少 `5756.99 m`。

### 4.3 定位与清除

在 25 站搜索过程中，已出现频道仍可利用后续“本来就要访问”的站点继续测向；一旦 `R_MEC<=60 m` 就停止对该已知频道补测。这样既利用了不增加全局搜索路程的顺路观测，又避免为过度收敛可行域反复支付 5--6 s 的检测与换频时间。

题目规定一局至多有 16 个干扰源。因此一旦已经发现 16 个不同频道，剩余频道必然无源，立即终止全局搜索并转入逐源定位；少于 16 个源时仍完整访问 25 站并验证所有未知频道。该停止条件只使用题设上界，不是概率猜测，不影响找全保证。旧策略在 16 源日志中仍访问全部 35 站，单局 `9604.37 s` 中移动就占 `7280.37 s`，这是最主要的冗余来源。

对仍只有一个或交会条件差的定向源，先利用一个对称选站定理。设 `A` 是已经收到该源信号的站点，在 `A` 清除失败后可知真实距离 `r>20 m`。令 `u` 为实测示向单位向量，`u_perp` 为其法向量，选择

\[
Q_\pm=A+19u\pm170u_\perp.
\]

考虑真实示向相对 `u` 最多偏差 `1°`。在最不利 `r=1000 m` 处，两个候选点到源的距离仍小于 1000 m；当 `r>1000 m` 时，原检测成功又说明该源自身接收半径至少为 `r`，两候选点同样不超出接收半径。对任意能覆盖 `A` 的 180° 发射半平面，`Q_+` 和 `Q_-` 关于示向轴分居两侧，且沿源方向的前移只有 19 m，小于已知的 `r`，故至少一个仍在发射半平面内。按离当前位置较近者先检测，最多两次 `/measure` 必获得一条新示向线。

程序反复使用该对称点对并更新定位多边形。若最小包围圆半径降至 19 m 就一次清除；若剩余多边形只需不超过 12 个清除覆盖点，则直接覆盖更省时。最终保底仍是用半径 19 m 的三角格覆盖完整可行域并逐点清除。题目明确 `/clear` 与定向覆盖方向无关，因此该步骤对任何定向方向均有效且必然清除。

25 站、20 频道全部检测的保守搜索上界为

\[
T_{search,4}\le18178.83/5+25\times20\times5+(500-1)
=6634.77\text{ s}.
\]

相对上一版 25 站平移晶格上界 `7786.16 s` 再减少 `1151.39 s`。这是保证所有极端朝向源都被发现的上界，不是典型运行时间。在同一组 100 个 10--16 源严格配对离线测试中，双环 25 站、少中心晶格 25 站和含中心晶格 26 站均全部清除。双环方案平均总时间为 `7161.35 s`，少中心晶格 25 站为 `8258.56 s`，含中心晶格 26 站为 `8348.74 s`；双环方案相对两者均为 98 组更快、2 组更慢，平均分别节省 `1097.22 s` 和 `1187.40 s`。10 个恰有 16 源的样例中，双环方案平均为 `5806.98 s`，即 `362.94 s/源`。另行构造的 10 源、16 源全边界朝外案例也用于验证找全与早停分支。

日志还表明，对已经发现的问题 4 频道继续在搜索站补测直至 MEC 半径降到 19 m，会消耗大量 5 s 测向。改为在 `R_MEC<=60 m` 时停止顺路补测，随后仍使用对称保证接收点和完整可行域覆盖完成定位清除。晶格布局下重新比较 19、30、40、50、60、70、80、100 m 八档阈值，60 m 与均值最优的 50 m 仅差约 `5.4 s`，但最差样例更稳，故保留 60 m。该改动不删减未知频道的完整搜索，因而不影响找全保证。

## 5. 算法流程与停止条件

```text
/enter
生成问题3的7站序列或问题4的25站序列
for 搜索站:
    若问题4已发现16个不同频道：停止全局搜索
    构造仍需确认的频道集合
    按蛇形次序逐频道 /measure
    direction -> 更新角域、1500m圆约束和MEC
    near      -> 原地 /clear，并删除频道
for 每个已发现但未清除的频道（按最近目标区域排序）:
    问题3：在保证接收候选区内迭代选站，直至MEC可清除
    问题4：利用搜索阶段交会结果，并以对称点对保证新增示向线
    若 MEC半径 <= 20m：在圆心 /clear
    否则：用19m三角格覆盖完整可行域，逐点 /clear 至成功
/exit
```

停止扫描某个未知频道只有两种合法情况：其搜索覆盖已完成，或已经发现 16 个互异频道。停止整个任务的条件是所有已发现源均收到 `clear_result="success"`，且所有其余频道均完成相应的全局发现覆盖。

## 6. 文件、验证与测试统计

- `localization_geometry.py`：角域裁剪、定位多边形直径、Welzl 最小包围圆、保证接收的第二站选址、19 m 清除覆盖网。
- `robot_strategy.py`：问题 3/4 的自适应 HTTP 策略和 JSONL 指令日志。
- `output/p3_policy.json`、`output/p4_policy.json`：不连接模拟器即可审阅的策略文件。

已完成的纯数学离线验证包括：

- 问题 3 搜索覆盖最坏采样距离 `999.1107 m`；
- 问题 4 的双环 25 站完成 518401 点连续邻域证书：单元半径 `4.6675 m`，最小凸包边界裕量 `11.1109 m`；
- 256 组随机合法真源与误差均始终位于计算可行域内；
- 32 组随机首次测向案例中的第二点均通过未知接收半径最坏值检验，最小接收裕量为 `74.15 m`；
- 构造点 `(600,700)` 验证了新候选域严格大于旧的固定 1000 m 圆盘交集；
- 19 m 三角清除网对上述可行域均满足覆盖；
- 等边三角形的 MEC 半径验证为 `D/sqrt(3)`。

按用户要求，本阶段没有启动官方模拟器，因此不能填写演练/正式测试的清除比例和平均时间。演练后应从 JSONL 最后一条成功响应读取 `virtual_time_s`，结合模拟器显示的真源总数计算

\[
\eta=N_{clear}/N_{total},\qquad
\bar T=T_{virtual}/N_{clear}.
\]

## 7. 参考文献

1. Torrieri, D. J. Statistical Theory of Passive Location Systems. *IEEE Transactions on Aerospace and Electronic Systems*, AES-20(2):183--198, 1984. DOI: `10.1109/TAES.1984.310439`.
2. Zhao, S., Chen, B. M., Lee, T. H. Optimal Placement of Bearing-Only Sensors for Target Localization. *2012 American Control Conference*:5108--5113. DOI: `10.1109/ACC.2012.6314884`.
3. Bishop, A. N., Fidan, B., Anderson, B. D. O., Dogancay, K., Pathirana, P. N. Optimality Analysis of Sensor-Target Geometries in Passive Localization: Part 1--Bearing-Only Localization. *ISSNIP 2007*:7--12. DOI: `10.1109/ISSNIP.2007.4496811`.
4. He, L., Gong, P., Zhang, X., Wang, Z. The Bearing-Only Target Localization via the Single UAV: Asymptotically Unbiased Closed-Form Solution and Path Planning. *IEEE Access*, 2019. DOI: `10.1109/ACCESS.2019.2947455`.
5. Welzl, E. Smallest Enclosing Disks (Balls and Ellipsoids). *Lecture Notes in Computer Science* 555:359--370, 1991. DOI: `10.1007/BFb0038202`.
6. Gaspar, Z., Tarnai, T., Hincz, K. Partial Covering of a Circle by 6 and 7 Congruent Circles. *Symmetry*, 13(11):2133, 2021. DOI: `10.3390/sym13112133`.
7. Bishop, A. N., Fidan, B., Anderson, B. D. O., Dogancay, K., Pathirana, P. N. Optimality Analysis of Sensor-Target Localization Geometries. *Automatica*, 46(3):479--492, 2010. DOI: `10.1016/j.automatica.2009.12.003`.
8. Bayram, H., Vander Hook, J., Isler, V. Gathering Bearing Data for Target Localization. *IEEE Robotics and Automation Letters*, 1(1):369--374, 2016. DOI: `10.1109/LRA.2016.2521387`.
